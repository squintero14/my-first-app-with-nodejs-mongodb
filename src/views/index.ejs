<%- include ('partials/_header'); %>
    <div class="container">
        <div class="row">
            <!-- FORM-->
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/add" method="POST">
                            <div class="form-group">
                                <input type="text" name="name" placeholder="Inserte un nombre" class="form-cotrol">
                            </div>
                            <button type="submit" class="bin btn-primary btn-block">
                                Agregar
                            </button>
                        </form>
                    </div>
                </div>

            </div>
            <!-- LIST -->
            <div class="col-md-7">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>N°</th>
                            <th>Nombre</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i = 0; i < peoples.length; i++) { %>
                            <tr> 
                                <td> <%= i + 1 %></td>
                                <td> <%= peoples[i].name %></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
                
            </div>
        </div>
    </div>

<%- include ('partials/_footer'); %>



